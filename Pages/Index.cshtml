@page
@model IndexModel
@{
    ViewData["Title"] = "Doma";
}

<div>
    <h1>Výdaje</h1>
    
    <ul>
        @foreach (var item in Model.Expenses)
        {
            <li>@item.Amount @item.Description @item.CategoryName @item.Created</li>
        }
    </ul>

    <form>
        <label>
            Částka
            <input type="number"/>
        </label>
        <label>
            Popis
            <input type="text"/>
        </label>
        <label>
            Kategorie
            <select>
                @foreach (var category in Model.UserCategories)
                {
                    <option value="@category.Id">@category.Name</option>
                }
            </select>
        </label>
        <label>
            Datum
            <input type="date" value="@DateTime.Today.ToString("yyyy-MM-dd")"/>
        </label>
        <button>
            Vytvořit
        </button>
    </form>
</div>